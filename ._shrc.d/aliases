# Clear orphaned packages and package cache
alias clean='sudo apt autoremove && sudo apt clean && flatpak uninstall --unused'

# Activate python environment
alias activate='source ./venv/bin/activate'

# Send to trash (safer alternative to rm)
alias trash='gio trash'

# Quick speedtest logger
alias quick_speedtest_log='python3 ~/my_projects/speedtest-logger/quick_speedtest_log.py'

# Add ESP32 toolchain to PATH
alias get-idf='source ~/my_projects/embedded/esp-idf/export.sh'

# Run job viewer server
alias job-viewer-server-run='cd ~/my_projects/filter_and_view_jobs/ && activate && uvicorn backend.main:app'

# Run reactive-resume server
alias reactive-resume-server-run='cd ~/my_projects/resume/Reactive-Resume && pnpm dev'

# Run script for querying gemini database csv
alias query-jd-csv='python3 ~/system_scripts/cli_utils/csv_query_get_filename.py'

# Show frequency analysis file
alias freq-analysis-show='moderncsv ~/my_projects/2025_job_skills_analysis/html_based/freq_linkedin_all_developer.csv &>/dev/null &'

# Views analysis
alias views-analysis-print='cd ~/my_projects/views_analysis && activate && python3 pandas_db_show.py && deactivate'

# Fix capslock delay manually
alias fix-capslock='~/system_scripts/desktop_settings/fix-capslock-delay.sh'

# copy to clipboard on linux
alias xc='xclip -sel clip'

# check and log distro size
alias distro_size='~/system_scripts/disc_usage/distro_size.sh'

# asciinema
alias rec='asciinema rec'
alias play='asciinema play'
alias upload='asciinema upload'

# big-AGI
# alias agi-dev='cd /home/a-victus/my_projects/LLMs/big-AGI-vercel && npm run dev &'
alias agi-regular-backup='~/system_scripts/backups/big-agi-regular-backup.sh'
alias agi-backups-dir='cd /mnt/d/Coding/LLMs/big-AGI/chat\ backups\ \(export\ all\)'

# 42 compilation and norm
alias cc='cc -Wall -Wextra -Werror'
# alias norminette='norminette -R CheckForbiddenSourceHeader -R CheckDefine'

# Create Dockerfile in the current folder
alias create_valgrind='echo "FROM alpine:latest
RUN apk add g++ valgrind make nano" > Dockerfile && docker build -t "valgrind:1.0" . && docker run -it -v $PWD:/tmp -w /tmp valgrind:1.0'

# add ssh identity for github repo
alias addid="ssh-add ~/.ssh/id_ed25519_personal"

# Refresh / activate new changes to .bashrc and .zshrc
alias refbash='source ~/.bashrc'
alias refzsh='source ~/.zshrc'

# Navigation
alias ..='cd ..'
alias ...='cd ../..'
alias ....='cd ../../..'
alias home='cd ~'
alias 42='cd ~/my_projects/42/core_curriculum/'
alias dotfiles='cd ~/dotfiles'
alias coding='cd /mnt/d/Coding/'
alias 2025='cd /mnt/d/Onedrive/My\ Folder/Job\ \&\ job\ applications/2025'
alias downloads='cd /mnt/c/Users/alimo/downloads'
alias job-txts='cd ~/my_projects/2025_job_skills_analysis/txt_based/txts/unhyphenated_txts'

# Open in VS Code
alias alimnaqvi='code ~/my_projects/alimnaqvi'

# Listing files
alias ll='ls -alFh'
alias la='ls -A'
alias l='ls -CF'

# File operations
# alias rm='rm -i'
# alias cp='cp -i'
# alias mv='mv -i'

# Grep
alias grep='grep --color=auto'
alias fgrep='fgrep --color=auto'
alias egrep='egrep --color=auto'

# System
alias update='sudo apt update && sudo apt upgrade'
alias ports='netstat -tulanp'

# Misc
alias h='history'
alias j='jobs -l'
alias path='echo -e ${PATH//:/\\n}'
alias now='date +"%T"'
alias nowdate='date +"%d-%m-%Y"'

# Git shortcuts
alias gs='git status'
alias ga='git add'
alias gc='git commit'
alias gp='git push'
# alias gl='git pull'

# Docker
alias dps='docker ps'
alias dimg='docker images'

# Python
alias py='python3'
# alias pip='pip3'

# File content
alias tf='tail -f'
# alias less='less -R'

# Quick edit of config files
alias vimrc='${EDITOR} ~/.vimrc'
alias bashrc='${EDITOR} ~/.bashrc'
alias zshrc='${EDITOR} ~/.zshrc'
alias aliases='${EDITOR} ~/dotfiles/._shrc.d/aliases'
alias gitconfig='${EDITOR} ~/.gitconfig'

# mkdir and cd into it
function mkcd {
  if [ ! -n "$1" ]; then
    echo "Enter a directory name"
  elif [ -d $1 ]; then
    echo "\`$1' already exists"
  else
    mkdir $1 && cd $1
  fi
}

# mini-moulinette
# alias mini='~/mini-moulinette/mini-moul.sh'

